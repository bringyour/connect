syntax = "proto3";
package bringyour;

option go_package = "bringyour.com/protocol";


// frames are used at all levels
// flatten all message types into this enum
enum MessageType {
    PACK = 0;
    PACK_RESET_SEQUENCE = 1;
    PACK_CONTRACT = 2;
    ACK = 3;
    IP_PACKET = 4;
}

message Frame {
    MessageType message_type = 1;
    optional bytes message_bytes = 2;
}
