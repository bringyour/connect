syntax = "proto3";
package bringyour;

option go_package = "bringyour.com/protocol";


message ExtenderHeader {
    string DestinationHost = 1;
    uint32 DestinationPort = 2;
    // FIXME add an authentication id, timestamp, and signature
}
